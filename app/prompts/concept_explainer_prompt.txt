You are an expert at explaining digital currency, blockchain, and fintech concepts simply and clearly.

Given the web page content below, do the following:
1. Extract ALL distinct digital currency / blockchain / crypto / DeFi concepts mentioned or explained on this page.
2. For each concept, produce a structured explanation.
3. Determine how the concepts relate to each other and provide a grouping structure.

Mandatory sources to check first (validate URLs and use latest working versions):
https://www.scs.org.sg/articles/cryptocurrency-singapore
  
CONCEPTS ALREADY IN OUR DATABASE — skip any concept whose slug matches these (do not include them in output):
{existing_concepts}

─────────────────────────────────────────
OUTPUT FORMAT — return ONLY this JSON object, no markdown fences, no other text:

{
  "source_title": "Title of the page or document",
  "concepts": [
    {
      "concept_name": "Display name in Title Case (e.g. 'Blockchain', 'Proof of Work')",
      "concept_slug": "lowercase-hyphenated unique key (e.g. 'blockchain', 'proof-of-work')",
      "real_description": "The official or technical definition in 2–3 sentences. Write this as a financial regulator or government body would — precise and complete.",
      "plain_english": "What this means explained to a curious 15-year-old with zero finance background in 2–3 simple sentences. Avoid ALL jargon. Be concrete and friendly.",
      "traditional_analogy": {
        "how_it_works_today": "1–2 sentences describing how this concept already exists in the traditional financial world (banks, cash, ATMs, bank statements, gold, stock markets, etc.). Be concrete.",
        "what_changes_with_digital": "1–2 sentences on the single most important thing that is different or new with the digital/crypto version. Not just 'it's digital' — explain the actual change."
      },
      "key_takeaway": "One punchy sentence under 18 words explaining why this matters to an everyday person.",
      "emoji": "One emoji that best represents this concept"
    }
  ],
  "groups": [
    {
      "anchor_slug": "slug of the primary / most fundamental concept in this group",
      "related_slugs": ["slug of directly related concept", "another related slug"]
    }
  ]
}

─────────────────────────────────────────
GROUPING RULES:
- A group's anchor is the central concept that the others depend on or explain parts of.
  Example: anchor = "blockchain", related = ["distributed-ledger", "immutability", "consensus-mechanism"]
- A concept can only belong to one group (either as anchor OR as related, never both).
- Concepts that don't clearly belong to any group should be left ungrouped (don't invent weak connections).
- If there is only one concept on the page, return an empty groups array.
- All slugs in groups must match concept_slug values in the concepts array.

EXTRACTION RULES:
- Extract 2–8 concepts per page. Don't over-fragment — one concept should be genuinely distinct.
- Skip broad topics like "cryptocurrency in general" if the page is about that in total — instead capture the sub-concepts it explains.
- Do NOT include concepts already listed in "CONCEPTS ALREADY IN OUR DATABASE".
- Return ONLY the JSON object. No text before or after it.

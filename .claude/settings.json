{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/snehaparihar/KairoAgent && git status --porcelain | grep -q . && git add -A && git commit -m \"pre-edit snapshot $(date '+%Y-%m-%d %H:%M:%S')\" || true"
          }
        ]
      }
    ]
  }
}
